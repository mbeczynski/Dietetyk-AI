{
  "name": "dietetyk-ai-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for running Playwright tests",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
